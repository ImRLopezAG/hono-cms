{
  "name": "@hono-cms/example-cloudflare-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "dev:wrangler": "wrangler dev --port 8789 --local",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist coverage .wrangler"
  },
  "dependencies": {
    "@hono-cms/adapter-memory": "workspace:*",
    "@hono-cms/cache": "workspace:*",
    "@hono-cms/core": "workspace:*",
    "@hono-cms/platform": "workspace:*",
    "@hono-cms/schema": "workspace:*",
    "@hono-cms/storage-memory": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.0.0",
    "wrangler": "^4.55.0"
  }
}
