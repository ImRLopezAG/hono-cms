{
  "name": "@hono-cms/example-bun-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun src/index.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "bun test src/index.test.ts",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "@hono-cms/adapter-memory": "workspace:*",
    "@hono-cms/cache": "workspace:*",
    "@hono-cms/core": "workspace:*",
    "@hono-cms/jobs": "workspace:*",
    "@hono-cms/schema": "workspace:*",
    "@hono-cms/storage-memory": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0"
  }
}
