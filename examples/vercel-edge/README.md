# Vercel Edge Example

This example exports a Vercel Edge route handler backed by Hono CMS and the Web `Request`/`Response` API.

```ts
export const runtime = "edge";
export { handler as GET, handler as POST };
```

It also exports a `vercel.json` cron shape generated by `@hono-cms/platform`.
